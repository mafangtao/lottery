{
  "name": "lottery",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "npx truffle test",
    "console": "npx truffle console --network local",
    "node": "npx truffle develop --log",
    "copy": "cp ./build/contracts/Lottery.json ./frontend/src/contract/",
    "prebuild": "npx truffle compile --all",
    "reset": "npx truffle migrate --f 1 --to 2 --network local"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@openzeppelin/truffle-upgrades": "^1.7.0",
    "bootstrap": "^5.0.1",
    "chai": "^4.3.4",
    "truffle": "^5.3.6",
    "web3": "^1.3.6"
  }
}
